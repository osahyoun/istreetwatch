<% content_for :title do %>
  Reports
<% end %>

<div class="row section">
  <div class="col s12">

    <p><strong><%= @reports.approved.count %></strong> of <strong><%= @reports.count %></strong> reports have been approved.</p>

    <% unless @reports.empty? %>
      <%= will_paginate @reports, class: 'center' %>

      <%= render 'table', reports: @reports %>

      <%= will_paginate @reports, class: 'center' %>
    <% end %>

  </div>
</div>